---
phase: 01-skeleton-env-verify
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/tsconfig.json
  - frontend/tsconfig.app.json
  - frontend/tsconfig.node.json
  - frontend/index.html
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/App.css
  - frontend/src/pages/DemoPage.tsx
  - frontend/src/services/api.ts
  - frontend/src/vite-env.d.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "React 前端应用启动后可通过 Vite proxy 访问后端 API，无 CORS 错误"
    - "前端联调 demo 页面可显示从后端获取的测试数据"
    - "npm run dev 可启动前端开发服务器（端口 5173）"
    - "Vite proxy 配置正确，/api 请求代理到后端 8000 端口"
  artifacts:
    - path: "frontend/package.json"
      provides: "前端项目配置与依赖声明"
      contains: "antd"
    - path: "frontend/vite.config.ts"
      provides: "Vite 配置 + API proxy 到后端"
      contains: "proxy"
    - path: "frontend/src/App.tsx"
      provides: "React 应用入口 + 路由 + Ant Design 配置"
      exports: ["App"]
    - path: "frontend/src/pages/DemoPage.tsx"
      provides: "联调 demo 页面（显示后端数据）"
    - path: "frontend/src/services/api.ts"
      provides: "统一 API 请求层"
      exports: ["fetchHealth", "fetchDemoData"]
  key_links:
    - from: "frontend/vite.config.ts"
      to: "http://localhost:8000"
      via: "server.proxy /api → target"
      pattern: "proxy.*target.*localhost:8000"
    - from: "frontend/src/pages/DemoPage.tsx"
      to: "frontend/src/services/api.ts"
      via: "import { fetchHealth, fetchDemoData }"
      pattern: "import.*fetchHealth.*fetchDemoData"
    - from: "frontend/src/services/api.ts"
      to: "/api/v1"
      via: "fetch API_BASE path"
      pattern: "API_BASE.*api/v1"
---

<objective>
初始化 React 前端项目，配置 Vite proxy 联调后端 API，创建联调 demo 页面验证前后端通信。

Purpose: 确认前后端联调全链路可用——前端通过 proxy 请求后端 API 无 CORS 错误，数据可正确显示。这是后续所有前端开发的基础。
Output: 可运行的 React 前端应用，联调 demo 页面显示后端测试数据。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@AGENTS.md

<!-- 依赖 Plan 01-01 的输出 -->
@.planning/phases/01-skeleton-env-verify/01-01-SUMMARY.md

<interfaces>
<!-- 前端骨架的核心接口定义。执行器按此实现 -->

<!-- Vite proxy 配置模式（来自 ARCHITECTURE.md Pattern 4） -->
```javascript
// frontend/vite.config.ts
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  server: {
    port: 5173,
    proxy: {
      '/api': {
        target: 'http://localhost:8000',
        changeOrigin: true,
      }
    }
  }
})
```

<!-- API service 层模式（来自 ARCHITECTURE.md Pattern 5） -->
```typescript
// frontend/src/services/api.ts
const API_BASE = '/api/v1';  // 开发时 Vite 代理，部署时同域

async function fetchJSON(path: string, options = {}) {
  const response = await fetch(`${API_BASE}${path}`, {
    headers: { 'Content-Type': 'application/json' },
    ...options,
  });
  if (!response.ok) {
    throw new Error(`API Error: ${response.status} ${response.statusText}`);
  }
  return response.json();
}

export const fetchHealth = () => fetchJSON('/health');
export const fetchDemoData = () => fetchJSON('/demo');
```

<!-- Ant Design 中文本地化配置 -->
```typescript
import zhCN from 'antd/locale/zh_CN';
import { ConfigProvider } from 'antd';

<ConfigProvider locale={zhCN}>
  <App />
</ConfigProvider>
```

<!-- 后端 API 接口（来自 Plan 01-01 已创建的端点） -->
后端已在 Plan 01-01 中创建：
- GET /health → {"status": "ok", "service": "AI工坊平台", "version": "0.1.0"}
- GET /api/v1/demo → [{"id": 1, "name": "测试项目", "description": "联调测试数据"}]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 初始化 React 前端项目并安装依赖</name>
  <files>frontend/package.json, frontend/tsconfig.json, frontend/tsconfig.app.json, frontend/tsconfig.node.json, frontend/index.html, frontend/src/vite-env.d.ts, frontend/vite.config.ts</files>
  <read_first>AGENTS.md（技术栈约定），.planning/research/STACK.md（前端依赖列表与版本）</read_first>
  <action>
    1. 在项目根目录创建前端项目：使用 `npm create vite@latest frontend -- --template react-ts`
       - 注意：此命令在 action_learning 根目录下执行，frontend 目录由 Vite 脚手架创建
       - 使用 `--template react-ts` 确保 TypeScript 支持（STACK.md 推荐 TypeScript 5.x）

    2. 安装核心依赖（在 frontend 目录下执行）：
       - `npm install antd @ant-design/icons zustand axios dayjs react-router`

    3. 安装开发依赖：
       - `npm install -D @types/node`

    4. 修改 frontend/vite.config.ts 配置：
       ```typescript
       import { defineConfig } from 'vite'
       import react from '@vitejs/plugin-react'

       export default defineConfig({
         plugins: [react()],
         server: {
           port: 5173,
           proxy: {
             '/api': {
               target: 'http://localhost:8000',
               changeOrigin: true,
             }
           }
         }
       })
       ```
       - 这是 ARCHITECTURE.md Pattern 4 的核心配置
       - `/api` 请求代理到 FastAPI 后端 8000 端口
       - 这是 PITFALLS.md Pitfall 8 的预防措施（开发时前端通过代理请求后端，完全避开 CORS）

    5. 验证安装成功：`cd frontend && npm run build` 应无错误
    6. 验证开发服务器可启动：`cd frontend && npm run dev`（启动后访问 http://localhost:5173 确认页面加载）
  </action>
  <verify>
    <automated>cd frontend && npm run build && echo "BUILD_PASS"</automated>
  </verify>
  <done>
    frontend/package.json 包含 antd、@ant-design/icons、zustand、axios、dayjs、react-router 依赖；vite.config.ts 配置了 proxy；npm run build 成功
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建前端应用骨架（入口 + Ant Design 配置 + 联调 demo 页面 + API service）</name>
  <files>frontend/src/main.tsx, frontend/src/App.tsx, frontend/src/App.css, frontend/src/pages/DemoPage.tsx, frontend/src/services/api.ts</files>
  <read_first>.planning/research/ARCHITECTURE.md（Pattern 5: 前端统一 API Service 层），frontend/vite.config.ts（proxy 配置确认），frontend/src/main.tsx（Vite 脚手架生成的初始内容）</read_first>
  <action>
    修改/创建前端核心文件：

    1. **frontend/src/main.tsx** — React 入口（修改 Vite 手架生成的文件）：
       ```typescript
       import React from 'react'
       import ReactDOM from 'react-dom/client'
       import App from './App.tsx'
       import zhCN from 'antd/locale/zh_CN'
       import { ConfigProvider } from 'antd'

       ReactDOM.createRoot(document.getElementById('root')!).render(
         <React.StrictMode>
           <ConfigProvider locale={zhCN}>
             <App />
           </ConfigProvider>
         </React.StrictMode>,
       )
       ```
       - ConfigProvider + zhCN 是 Ant Design 中文本地化配置（STACK.md 推荐）

    2. **frontend/src/services/api.ts** — 统一 API 请求层：
       ```typescript
       const API_BASE = '/api/v1'

       async function fetchJSON(path: string, options: RequestInit = {}): Promise<any> {
         const response = await fetch(`${API_BASE}${path}`, {
           headers: { 'Content-Type': 'application/json' },
           ...options,
         })
         if (!response.ok) {
           throw new Error(`API Error: ${response.status} ${response.statusText}`)
         }
         return response.json()
       }

       // 健康检查
       export const fetchHealth = () => fetchJSON('/health')

       // 联调 demo 数据
       export const fetchDemoData = () => fetchJSON('/demo')
       ```
       - 这是 ARCHITECTURE.md Pattern 5 的实现
       - API_BASE = '/api/v1'，开发时通过 Vite proxy 代理到后端

    3. **frontend/src/pages/DemoPage.tsx** — 联调 demo 页面：
       ```typescript
       import { useState, useEffect } from 'react'
       import { Card, Typography, Spin, Alert, Descriptions, Tag } from 'antd'
       import { fetchHealth, fetchDemoData } from '../services/api'

       const { Title, Paragraph } = Typography

       interface DemoItem {
         id: number
         name: string
         description: string | null
         created_at: string
       }

       interface HealthStatus {
         status: string
         service: string
         version: string
       }

       export default function DemoPage() {
         const [health, setHealth] = useState<HealthStatus | null>(null)
         const [demoData, setDemoData] = useState<DemoItem[]>([])
         const [loading, setLoading] = useState(true)
         const [error, setError] = useState<string | null>(null)

         useEffect(() => {
           async function loadData() {
             try {
               setLoading(true)
               const healthData = await fetchHealth()
               const items = await fetchDemoData()
               setHealth(healthData)
               setDemoData(items)
               setError(null)
             } catch (err) {
               setError(err instanceof Error ? err.message : '请求失败')
             } finally {
               setLoading(false)
             }
           }
           loadData()
         }, [])

         if (loading) return <Spin size="large" style={{ display: 'block', margin: '100px auto' }} />

         if (error) return <Alert type="error" message="联调失败" description={error} />

         return (
           <div style={{ padding: '24px', maxWidth: '800px', margin: '0 auto' }}>
             <Title level={2}>🛠 前后端联调 Demo</Title>
             <Paragraph>验证前端通过 Vite proxy 成功访问后端 API</Paragraph>

             <Card title="后端健康状态" style={{ marginBottom: '16px' }}>
               <Descriptions bordered column={1}>
                 <Descriptions.Item label="状态">
                   <Tag color="green">{health?.status}</Tag>
                 </Descriptions.Item>
                 <Descriptions.Item label="服务名称">{health?.service}</Descriptions.Item>
                 <Descriptions.Item label="版本">{health?.version}</Descriptions.Item>
               </Descriptions>
             </Card>

             <Card title="后端测试数据">
               {demoData.map((item) => (
                 <Card.Grid key={item.id} style={{ width: '100%', padding: '16px' }}>
                   <Title level={4}>{item.name}</Title>
                   <Paragraph>{item.description}</Paragraph>
                   <Tag>ID: {item.id}</Tag>
                 </Card.Grid>
               ))}
             </Card>
           </div>
         )
       }
       ```
       - 此页面是 ROADMAP Phase 1 Success Criteria #3 的实现：前后端联调 demo 页面可显示从后端获取的测试数据

    4. **frontend/src/App.tsx** — 应用入口 + 路由：
       ```typescript
       import { BrowserRouter, Routes, Route } from 'react-router'
       import DemoPage from './pages/DemoPage'
       import './App.css'

       function App() {
         return (
           <BrowserRouter>
             <Routes>
               <Route path="/" element={<DemoPage />} />
             </Routes>
           </BrowserRouter>
         )
       }

       export default App
       ```
       - Phase 1 只有一个 demo 页面路由，后续 Phase 会添加更多路由

    5. **frontend/src/App.css** — 基础样式（可保留 Vite 脚手架的默认样式）
  </action>
  <verify>
    <automated>cd frontend && npm run build && echo "BUILD_PASS"</automated>
  </verify>
  <done>
    frontend/src/main.tsx 包含 ConfigProvider + zhCN 本地化；App.tsx 包含 BrowserRouter + DemoPage 路由；
    services/api.ts 包含 fetchHealth 和 fetchDemoData 函数；
    DemoPage.tsx 可显示后端健康状态和测试数据；
    npm run build 成功
  </done>
</task>

<task type="auto">
  <name>Task 3: 验证前后端联调（启动后端 + 前端，确认 proxy 通信和数据显示）</name>
  <files>backend/tests/test_integration.py</files>
  <read_first>frontend/vite.config.ts（proxy 配置），backend/app/main.py（后端 CORS 和路由配置），frontend/src/services/api.ts（API 请求层）</read_first>
  <action>
    创建一个集成测试来验证前后端联调链路：

    1. 创建 backend/tests/test_integration.py：
       ```python
       from fastapi.testclient import TestClient
       from app.main import app

       client = TestClient(app)

       def test_health_endpoint_accessible_from_frontend_proxy():
           """验证 /health 端点可被前端通过 proxy 访问"""
           response = client.get("/health")
           assert response.status_code == 200
           data = response.json()
           assert data["status"] == "ok"

       def test_demo_endpoint_returns_list():
           """验证 /api/v1/demo 端点返回数组数据"""
           response = client.get("/api/v1/demo")
           assert response.status_code == 200
           data = response.json()
           assert isinstance(data, list)
           assert len(data) > 0
           assert data[0]["name"] == "测试项目"

       def test_cors_allows_frontend_origin():
           """验证 CORS 允许 http://localhost:5173 来源"""
           response = client.options(
               "/health",
               headers={
                   "Origin": "http://localhost:5173",
                   "Access-Control-Request-Method": "GET",
               }
           )
           assert response.headers.get("access-control-allow-origin") == "http://localhost:5173"
       ```

    2. 运行所有后端测试：`cd backend && uv run pytest tests/ -v`

    3. 启动后端服务：`cd backend && uv run uvicorn app.main:app --host 0.0.0.0 --port 8000`

    4. 启动前端开发服务器：`cd frontend && npm run dev`

    5. 在浏览器访问 http://localhost:5173：
       - 确认页面加载无错误
       - 确认 demo 页面显示后端健康状态（status: ok）
       - 确认 demo 页面显示测试数据（测试项目）
       - 确认浏览器 Console 无 CORS 错误

    6. 关闭两个服务
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/ -v</automated>
  </verify>
  <done>
    所有后端测试通过（health、demo、cors）；浏览器访问 http://localhost:5173 显示联调 demo 页面；
    页面显示后端健康状态（ok）和测试数据；浏览器 Console 无 CORS 错误
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 浏览器 → Vite dev server | 前端页面请求到达开发服务器 |
| Vite proxy → FastAPI | 代理请求转发到后端 API（开发环境信任边界） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | S (Spoofing) | Vite proxy | accept | 开发环境代理，仅 localhost 可访问，不暴露公网 |
| T-01-06 | I (Tampering) | npm packages | mitigate | 使用 npm audit 检查依赖安全性；package-lock.json 锁定版本 |
| T-01-07 | D (DoS) | Vite dev server | accept | 开发工具，仅本地使用 |
| T-01-08 | E (Elevation) | API proxy | accept | Proxy 仅转发 /api 路径请求，不暴露后端管理端口 |
</threat_model>

<verification>
1. `cd backend && uv run pytest tests/ -v` — 所有后端测试通过
2. `cd frontend && npm run build` — 前端构建成功
3. 启动后端 + 前端 → 浏览器访问 http://localhost:5173 → 页面显示数据
4. 浏览器 Console 无 CORS 错误
</verification>

<success_criteria>
- React 前端应用启动后可通过 Vite proxy 访问后端 /health API，无 CORS 错误
- 前端联调 demo 页面显示后端健康状态（status: ok）和测试数据（测试项目）
- npm run dev 可启动前端开发服务器
- Vite proxy 配置正确：/api 请求代理到 http://localhost:8000
</success_criteria>

<output>
After completion, create `.planning/phases/01-skeleton-env-verify/01-02-SUMMARY.md`
</output>