# Phase 2 MVP Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the first usable AI 工坊平台 workflow for projects, customer requirements, tags, and manual matching.

**Architecture:** Keep the current FastAPI app and add focused SQLModel models, schemas, and routers under `/api/v1`. Replace the frontend demo screen with a compact Ant Design workbench that talks to the new API through `frontend/src/services/api.ts`.

**Tech Stack:** FastAPI, SQLModel, SQLite, pytest, React, TypeScript, Vite, Ant Design.

---

### Task 1: Backend Models And CRUD API

**Files:**
- Modify: `backend/app/models.py`
- Create: `backend/app/schemas.py`
- Create: `backend/app/api/projects.py`
- Create: `backend/app/api/requirements.py`
- Create: `backend/app/api/tags.py`
- Create: `backend/app/api/matches.py`
- Modify: `backend/app/main.py`
- Test: `backend/tests/test_phase2_api.py`

- [x] Write failing API tests for project, requirement, tag, and match creation/listing.
- [x] Run `uv run pytest tests/test_phase2_api.py -v` and verify failures are missing routes/models.
- [x] Implement SQLModel tables and Pydantic-style request/response schemas.
- [x] Implement routers with CRUD list/create/get/update behavior and match foreign-key validation.
- [x] Mount routers in `backend/app/main.py`.
- [x] Run `uv run pytest` and verify all backend tests pass.

### Task 2: Frontend API Client And Types

**Files:**
- Modify: `frontend/src/services/api.ts`
- Create: `frontend/src/types.ts`

- [x] Add TypeScript types matching backend response shapes.
- [x] Add typed API functions for projects, requirements, tags, and matches.
- [x] Run `npm run build` and verify TypeScript catches no type errors.

### Task 3: Frontend Workbench UI

**Files:**
- Modify: `frontend/src/App.tsx`
- Create: `frontend/src/pages/WorkbenchPage.tsx`
- Modify: `frontend/src/App.css`
- Modify: `frontend/src/index.css`

- [x] Replace the demo route with the workbench page.
- [x] Build tabs for projects, requirements, tags, and matches.
- [x] Add create forms and tables for all Phase 2 entities.
- [x] Add manual match creation from existing project and requirement records.
- [x] Run `npm run build` and verify production build succeeds.

### Task 4: Planning State Update

**Files:**
- Modify: `.planning/STATE.md`
- Modify: `.planning/ROADMAP.md`

- [x] Mark Phase 2 plans complete when implementation and verification pass.
- [x] Record that Phase 3 is the next focus.
